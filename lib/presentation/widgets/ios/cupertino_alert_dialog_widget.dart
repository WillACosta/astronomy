import 'package:flutter/cupertino.dart';

class CupertinoAlertDialogComponent extends StatelessWidget {
  const CupertinoAlertDialogComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
